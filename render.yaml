services:
  - type: web
    name: incident-triage-api
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.service.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GITHUB_TOKEN
        sync: false